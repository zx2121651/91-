<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Aurelian" parent="android:Theme.Material.NoActionBar">
        <item name="android:colorPrimary">@color/gold</item>

        <item name="android:colorBackground">@color/deep_black</item>
        <item name="android:statusBarColor">@color/deep_black</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>
</resources>
