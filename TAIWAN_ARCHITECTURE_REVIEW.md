# Aurelian 專案架構復盤（台灣營運場景）

> 範圍：重新定位為「台灣地區運行」後，評估目前程式碼架構、可營運性與主要風險。

## 1) 目前專案定位

此專案仍屬於 MVP / Prototype：

- Android 客戶端：Jetpack Compose + Retrofit + Media3
- 後端：Express 路由聚合 + in-memory mock 資料

目前已具備主要頁面流程（探索、配對、私訊、活動、速約），但在「可正式營運」所需的合規、風控、持久化、可觀測性方面仍不足。

---

## 2) 程式架構現況

### 後端

`server.js` 目前做了中介層與路由匯總，模組切分方向正確（auth/feed/messages/hookups...）。

但核心限制是：

- 多數業務邏輯仍直接寫在 route 層
- 無 service/repository 分層
- 無資料庫與快取層（如 PostgreSQL + Redis）
- 錯誤碼與驗證規則缺乏統一規範

### 客戶端

`AurelianApp.kt` 導覽結構清楚，Hookups 已整合至底部導航。

目前主要技術債：

- `Network.kt` 過度集中（模型與 API 介面高度耦合）
- Base URL 寫死為 emulator host（`10.0.2.2`）
- 缺統一攔截器（token 注入、request-id、重試策略）

### Hookups 模組

功能相較初版已包含：

- 篩選（city / intent）
- 分頁（cursor / limit）
- 發送邀約與狀態查詢
- 前端輪詢狀態

但仍為 mock 等級：狀態存在記憶體 Map，服務重啟資料即遺失，不適合正式營運。

---

## 3) 台灣場景下的重點問題

### 3.1 法規與平台合規（高優先）

若在台灣營運，需優先補齊：

1. **個資保護法（PDPA）落地**
   - 資料蒐集目的告知、同意管理、刪除與查詢機制。
   - 資料最小化與保存期限政策。

2. **高風險社交場景防護**
   - 檢舉、封鎖、騷擾防護、風險帳號分級。
   - 對邀約行為做頻率限制與異常檢測。

3. **內容治理**
   - 文字/圖片/影片審核流程（機審 + 人審）。
   - 明確使用者規範與違規處理流程。

### 3.2 系統穩定性與可營運性

1. 缺資料持久化與審計記錄，無法追溯爭議事件。
2. 缺觀測性（trace-id、結構化 log、告警與 dashboard）。
3. API 契約需再收斂（避免路由與客戶端預期不一致）。

### 3.3 在地化體驗

目前資料包含中英混合（如城市 Shanghai/Beijing），對台灣在地產品感仍不足。

建議補齊：

- 城市與行政區在地詞彙（台北/新北/台中/高雄…）
- 時區與日期格式統一（Asia/Taipei）
- 繁體中文文案與在地活動語境

---

## 4) 建議落地路線（台灣版）

### Phase A（先上線底座）

- 完整身分與會話機制（JWT + refresh + device binding）
- 檢舉/封鎖/風險評分資料模型
- 最小可用內容審核流程

### Phase B（後端服務化）

- controller / service / repository 分層
- PostgreSQL + Redis
- 邀約、私訊、封鎖等關鍵行為審計表

### Phase C（客戶端治理）

- 拆分 Network 領域模組
- ViewModel 透過 Repository/UseCase，不直呼 API
- 統一錯誤處理與重試 UX

### Phase D（台灣在地化與營運）

- 全站繁體中文與在地城市資料
- 風控策略與客服 SOP 串接
- KPI 監控（留存、邀約成功率、檢舉率、封鎖率）

---

## 5) 總結

若以台灣為主要營運市場，專案方向可行，但應把優先級放在：

1. 合規與風控、2. 持久化與審計、3. API 契約治理、4. 在地化體驗。

先補底座，再擴功能，才能從 demo 進入可持續營運。
